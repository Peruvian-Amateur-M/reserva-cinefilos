<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Documento de Venta</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9"
      crossorigin="anonymous"
    />
    <link
      href="https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        margin: 20px;
        background-color: #f0f0f0;
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
      }
      .container {
        background-color: white;
        border: 2px solid #ddd;
        border-radius: 10px;
        padding: 20px;
        width: 80%;
        max-width: 600px;
        box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2);
        animation: dropAnimation 0.5s ease forwards;
      }
      h1,
      h2,
      h3 {
        text-align: center;
        font-weight: bold;
        color: #333;
      }
      ul {
        list-style-type: none;
        padding: 0;
      }
      li {
        margin-bottom: 10px;
        color: #555;
        font-size: 16px;
        line-height: 1.5;
      }
      .footer {
        text-align: center;
        margin-top: 20px;
      }
      .btn {
        background-color: #dc3545;
        border-color: #dc3545;
        color: white;
        font-weight: bold;
        padding: 10px 20px;
        font-size: 18px;
        border-radius: 5px;
      }
      .btn:hover {
        background-color: #c82333;
        border-color: #bd2130;
      }
      @keyframes dropAnimation {
        from {
          transform: translateY(-50%);
          opacity: 0;
        }
        to {
          transform: translateY(0);
          opacity: 1;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Documento de Venta</h1>

      <% if (pelicula) { %>
      <h2>Detalles de la Película</h2>
      <h3>Nombre: <%= pelicula.Titulo %></h3>
      <h3>Duración: <%= pelicula.Duracion %> minutos</h3>

      <h2>Detalles de los Asientos</h2>
      <ul>
        <% detallesAsientos.forEach(function(asiento) { %>
        <li>
          Asiento: <%= asiento.NumeroAsiento %>, Fila: <%= asiento.Fila %>,
          Precio: S/<%= asiento.Precio %>
        </li>
        <% }); %>
      </ul>

      <div class="footer">
        <a class="btn" href="/principal">Volver al inicio</a>
      </div>
      <% } else { %>
      <p>No se encontraron detalles de la película.</p>
      <% } %>
    </div>
  </body>
</html>
